<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nyaxs.nyastore.mapper.ManagersMapper">
  <resultMap id="BaseResultMap" type="com.nyaxs.nyastore.entity.Managers">
    <result column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="store_id" jdbcType="INTEGER" property="storeId" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="rank" jdbcType="INTEGER" property="rank" />
    <result column="email" jdbcType="VARCHAR" property="email" />
    <result column="phone" jdbcType="INTEGER" property="phone" />
  </resultMap>
  <insert id="insert" parameterType="com.nyaxs.nyastore.entity.Managers">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into managers (`name`, store_id, `password`, 
      `rank`, email, phone)
    values (#{name,jdbcType=VARCHAR}, #{storeId,jdbcType=INTEGER}, #{password,jdbcType=VARCHAR}, 
      #{rank,jdbcType=INTEGER}, #{email,jdbcType=VARCHAR}, #{phone,jdbcType=INTEGER})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, `name`, store_id, `password`, `rank`, email, phone
    from managers
  </select>
</mapper>